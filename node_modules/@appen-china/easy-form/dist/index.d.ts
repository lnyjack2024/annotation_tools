import React from 'react';
import ImageTextField from './components/ImageTextField';
import { Field, Condition, Effect, Rule, LayoutMode } from './types';
import utils from './utils';
interface FormProps {
    focusFieldKey?: string;
    theme?: string;
    fields: Field[];
    conditions?: Condition[];
    effects?: Effect[];
    rules?: Rule[];
    autoFocus?: boolean;
    hideLabel?: boolean;
    layoutMode?: LayoutMode;
    footerVisible?: boolean;
    submitText?: string;
    cancelText?: string;
    onSubmit: (values: any) => void;
    onFieldsChange?: (changedFields: any, allFields: any) => void;
    onCancel?: () => void;
    onChange?: (changedValues: any, formValues: any) => void;
    renderLabel?: (label: string | undefined, field: Field) => string | React.ReactNode;
    uploader?: (file: File) => Promise<string>;
}
declare const EasyForm: React.FC<FormProps>;
export default EasyForm;
export { utils, ImageTextField };
